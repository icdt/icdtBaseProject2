using System;
using System.Collections.Generic;
using System.Linq;
using System.Security.Claims;
using System.Security.Principal;
using System.Web;

namespace icdtBaseProject2.Identity
{
    public static class IdentityExtension
    {
        //public static UserType GetUserType(this IIdentity identity)
        //{
        //    //(Colors) Enum.Parse(typeof(Colors), colorString)
        //    var strUserType = ((ClaimsIdentity)identity).Claims.Single(c => c.Type == "UserType").Value;            
        //    return (UserType)Enum.Parse(typeof(UserType),strUserType);
        //}
    }
}